services:
  - type: web
    name: sontag-pos-backend
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /api/health/ready
    dockerfilePath: backend/Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sontag-pos-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true

databases:
  - name: sontag-pos-db
    plan: free
    databaseName: pos
    user: posuser
